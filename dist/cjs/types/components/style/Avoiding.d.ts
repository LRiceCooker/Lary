/// <reference types="react" />
import { laryComponentPassedPropsType } from '../../types/utils';
declare const Avoiding: ({ passedProps, children }: {
    passedProps: laryComponentPassedPropsType;
    children: JSX.Element;
}) => JSX.Element;
export default Avoiding;
